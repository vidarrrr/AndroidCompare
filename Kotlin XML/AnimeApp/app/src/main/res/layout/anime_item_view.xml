<?xml version="1.0" encoding="utf-8"?>
<androidx.cardview.widget.CardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    app:cardCornerRadius="24dp"
    android:layout_margin="8dp"
    >
<androidx.constraintlayout.widget.ConstraintLayout android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:padding="12dp"
    android:id="@+id/anime_constraint_layout"
    android:background="@drawable/background"
    >

    <ImageView
        android:layout_width="64dp"
        android:layout_height="64dp"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintLeft_toLeftOf="parent"
        android:id="@+id/anime_image_view"
        android:scaleType="centerCrop"
        android:contentDescription="@string/anime_image"/>

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:maxLines="1"
        android:text="@string/anime_image"
        android:layout_marginStart="4dp"
        android:id="@+id/anime_name_text_view"
        android:textStyle="bold"
        android:textSize="18sp"
        android:ellipsize="end"
        android:textColor="@color/text_color"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintLeft_toRightOf="@id/anime_image_view"/>

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:maxLines="2"
        android:text="@string/anime_image"
        android:layout_marginStart="4dp"
        android:id="@+id/anime_details_text_view"
        android:textStyle="italic"
        android:textSize="16sp"
        android:ellipsize="end"
        android:textColor="@color/text_color"
        app:layout_constraintTop_toBottomOf="@id/anime_name_text_view"
        app:layout_constraintLeft_toRightOf="@id/anime_image_view"/>



</androidx.constraintlayout.widget.ConstraintLayout>
</androidx.cardview.widget.CardView>